{
  "name": "ml-chat",
  "version": "1.0.0",
  "description": "A simple authentication chat system for Singapore and Malaysia users",
  "scripts": {
    "setup": "echo \"Setting up ML-CHAT development environment...\" && cd src/server && npm install && cd ../client && npm install && cd ../.. && echo \"Setup complete!\"",
    "dev:server": "cd src/server && npm run dev",
    "dev:client": "cd src/client && npm start",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "test": "cd src/server && npm test",
    "test:watch": "cd src/server && npm run test:watch",
    "build": "cd src/server && npm run build && cd ../client && npm run build",
    "docs": "echo \"Opening documentation...\" && start docs"
  },
  "keywords": [
    "chat",
    "authentication",
    "singapore",
    "malaysia"
  ],
  "author": "nilochan",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}